
import { initAchievementPatcher } from './achievementPatcher';

// Initialize the achievement patcher
export function initializeAchievementPatcher() {
  initAchievementPatcher();
}

// Initialize immediately on import
initializeAchievementPatcher();
